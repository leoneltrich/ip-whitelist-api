version: '3.8'

services:
  app:
    build: .
    network_mode: "host"
    cap_add:
      - NET_ADMIN
    environment:
      - JWT_SECRET=SuperSecretValue
      - FIREWALL_BACKEND=nftables
    volumes:
      - ./application.db:/app/application.db
