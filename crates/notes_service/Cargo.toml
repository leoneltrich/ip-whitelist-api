[package]
name = "notes_service"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
sqlx = { version="0.8.6", features = ["runtime-tokio", "sqlite", "macros"] }
async-trait = "0.1.89"
